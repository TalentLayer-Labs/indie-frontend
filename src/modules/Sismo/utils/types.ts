export type ISismoGroup = {
  description: string;
  id: string;
  userInGroup: boolean;
  name: string;
  specs: string;
  image: string;
  link: string;
};

export type ISismoBadge = {
  name: string;
  image: string;
  description: string;
};
